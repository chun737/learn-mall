<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.SeckillActivityMapper">
    <!-- 4.7.7 终止活动：仅未删除的活动可终止，同步维护 updated_at；影响行数由 Service 校验 -->
    <update id="stopSeckillById" parameterType="java.lang.Long">
        update seckill_activity
        set status = 2,
            updated_at = NOW()
        where id = #{id}
          and deleted = 0
    </update>

    <!--
        秒杀活动列表（3.6.5）：联查 product / product_sku 组装 VO。
        状态按时间窗口与 NOW() 比较过滤，不依赖 status 字段（status 无定时任务自动流转）。
        available_stock 不在此处取真实库存，实际剩余库存由 Service 层读 Redis 实时值。

        ⭐ p.status = 1：必须与商品详情 getDPs 的口径一致。
        详情页 SQL 是「p.deleted = 0 AND p.status = 1」，而这里是 LEFT JOIN 且只过滤了 deleted，
        结果就是「商品已下架」的活动照样进列表——用户点进去必然 404
        （getDPs 查库为空 → 写 60 秒空值占位 → 抛 NOT_FOUND「资源不存在」）。
        只加这一条即可同时排除两种情况：商品不存在/已逻辑删除时 p.status 为 NULL，
        而 NULL = 1 不成立，所以无需再写 p.id IS NOT NULL。
        管理端 selectAdminList 刻意不加：运营需要看到"活动还在但商品已下架"以便处理。
    -->
    <select id="selectSeckillList" resultType="com.mall.vo.SeckillActivityVO">
        SELECT
            sa.id              AS id,
            sa.activity_name   AS activityName,
            sa.product_id      AS productId,
            p.product_name     AS productName,
            p.main_image       AS mainImage,
            sa.sku_id          AS skuId,
            sku.specs          AS specs,
            sa.seckill_price   AS seckillPrice,
            IFNULL(sku.price, sa.seckill_price) AS originalPrice,
            sa.total_stock     AS totalStock,
            sa.available_stock AS availableStock,
            sa.per_limit       AS perLimit,
            sa.start_time      AS startTime,
            sa.end_time        AS endTime
        FROM seckill_activity sa
        LEFT JOIN product p     ON p.id = sa.product_id AND p.deleted = 0
        LEFT JOIN product_sku sku ON sku.id = sa.sku_id AND sku.deleted = 0
        WHERE sa.deleted = 0
          AND p.status = 1
        <if test="status != null">
            <choose>
                <when test="status == 0">
                    AND sa.start_time &gt; NOW()
                </when>
                <when test="status == 1">
                    AND sa.start_time &lt;= NOW() AND sa.end_time &gt;= NOW()
                </when>
                <when test="status == 2">
                    AND sa.end_time &lt; NOW()
                </when>
            </choose>
        </if>
        ORDER BY sa.start_time DESC
    </select>
    <!--
        秒杀活动详情（3.6.6）：前台用，口径与 selectSeckillList 保持一致——商品必须上架。
        管理端不用这条 SQL（见 selectAdminList），所以这里的过滤不会挡住运营排查异常活动。
    -->
    <select id="selectDetailById" resultType="com.mall.vo.SeckillActivityVO" parameterType="java.lang.Long">
        SELECT
            sa.id              AS id,
            sa.activity_name   AS activityName,
            sa.product_id      AS productId,
            p.product_name     AS productName,
            p.main_image       AS mainImage,
            sa.sku_id          AS skuId,
            sku.specs          AS specs,
            sa.seckill_price   AS seckillPrice,
            IFNULL(sku.price, sa.seckill_price) AS originalPrice,
            sa.total_stock     AS totalStock,
            sa.available_stock AS availableStock,
            sa.per_limit       AS perLimit,
            sa.start_time      AS startTime,
            sa.end_time        AS endTime
        FROM seckill_activity sa
                 LEFT JOIN product p     ON p.id = sa.product_id AND p.deleted = 0
                 LEFT JOIN product_sku sku ON sku.id = sa.sku_id AND sku.deleted = 0
        WHERE sa.deleted = 0
          AND sa.id = #{id}
          AND p.status = 1
    </select>

    <!-- 布隆过滤器预热数据源（SeckillBloomIndex.loadAllIds）：全部未删除活动 ID -->
    <select id="selectAllIds" resultType="java.lang.Long">
        SELECT id
        FROM seckill_activity
        WHERE deleted = 0
    </select>
    <select id="selectAdminList" resultType="com.mall.vo.SeckillAdminVO">
        SELECT
            sa.id              AS id,
            sa.activity_name   AS activityName,
            sa.product_id      AS productId,
            p.product_name     AS productName,
            p.main_image       AS mainImage,
            sa.sku_id          AS skuId,
            sku.specs          AS specs,
            sa.seckill_price   AS seckillPrice,
            IFNULL(sku.price, sa.seckill_price) AS originalPrice,
            sa.total_stock     AS totalStock,
            sa.available_stock AS availableStock,
            sa.per_limit       AS perLimit,
            sa.start_time      AS startTime,
            sa.end_time        AS endTime,
            sa.total_stock - sa.available_stock AS soldQuantity,
            sa.created_at AS createdAt
        FROM seckill_activity sa
                 LEFT JOIN product p     ON p.id = sa.product_id AND p.deleted = 0
                 LEFT JOIN product_sku sku ON sku.id = sa.sku_id AND sku.deleted = 0
        WHERE sa.deleted = 0
        ORDER BY sa.start_time DESC
    </select>

    <!--
        ⭐ 秒杀库存条件扣减（CAS）：MQ 消费者建单成功后调用，让 DB 侧库存跟上 Redis 的预扣。
        判断与写入在同一条 UPDATE 内，并发下不会扣成负数；影响行数 0 = DB 库存不足或活动已删除。
        调用方（OrderServiceImpl.addSeckillOrderBatch）对 0 行的处理是「只记 WARN，不影响建单结果」——
        Redis 的 Lua 预扣才是防超卖的唯一权威；该列按建表注释（marketing_schema.sql）定位是
        「对账 + Redis 丢数据后重新预热的兜底」，不能反向打断用户已抢到的单。
        显式写 updated_at = NOW()：与 ProductSkuMapper.deductStock 同一风格
        （列上虽有 ON UPDATE CURRENT_TIMESTAMP，但显式写不依赖隐式行为，语义更直白）。
    -->
    <update id="deductAvailableStock">
        UPDATE seckill_activity
        SET available_stock = available_stock - #{qty},
            updated_at      = NOW()
        WHERE id = #{id}
          AND deleted = 0
          AND available_stock &gt;= #{qty}
    </update>

    <!--
        ⭐ 秒杀库存回补（取消/超时/退款）：行内自增，并用 LEAST 封顶在 total_stock。
        为什么封顶：回补是低频路径，但幂等性无法完全保证（MQ 重投、人工重试都可能多补一次）。
        一旦 available_stock 超过 total_stock，后台对账字段 soldQuantity = total_stock - available_stock
        就会变成负数，运营看到的是"已售 -3 件"。封顶让异常值收敛为"已售 0"而不是负数。
        带 deleted = 0：已删除的活动不值得回补，此时影响行数 0 属正常。
    -->
    <update id="restoreAvailableStock">
        UPDATE seckill_activity
        SET available_stock = LEAST(available_stock + #{qty}, total_stock),
            updated_at      = NOW()
        WHERE id = #{id}
          AND deleted = 0
    </update>

    <!--
        ⭐ 按「SKU + 下单时刻」反查活动（回补路径专用）：订单表没有活动 ID 列，只能反查。
        精度依据：addSeckill 已强制「同一 SKU 不允许与未结束活动时间重叠」，
        故 sku_id + 下单时刻落在 [start_time, end_time] 内至多命中一条。
        仍写 ORDER BY ... LIMIT 1 兜底：历史脏数据或 SQL 直插的重叠活动不会导致
        selectOne 抛 TooManyResultsException——回补属于次要路径，不该因数据瑕疵打断取消流程。
        边界取闭区间（下单时刻等于 start_time 或 end_time 都算在活动期内），
        与 SeckillActivityServiceImpl.computeStatus 的状态判定口径保持一致。
    -->
    <select id="selectActivityIdBySkuAt" resultType="java.lang.Long">
        SELECT sa.id
        FROM seckill_activity sa
        WHERE sa.sku_id = #{skuId}
          AND sa.deleted = 0
          AND #{orderTime} &gt;= sa.start_time
          AND #{orderTime} &lt;= sa.end_time
        ORDER BY sa.start_time DESC
        LIMIT 1
    </select>
</mapper>
