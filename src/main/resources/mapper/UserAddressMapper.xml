<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.UserAddressMapper">

    <select id="listAddresses" resultType="com.mall.entity.UserAddress" parameterType="java.lang.Long">
        SELECT
            id,
            user_id,
            receiver_name,
            receiver_phone,
            province,
            city,
            district,
            detail_address,
            is_default,
            created_at,
            updated_at,
            deleted
        FROM user_address
        WHERE user_id = #{userId}
          AND deleted = 0
        ORDER BY is_default DESC, created_at DESC
    </select>
    <select id="getUserById" resultType="com.mall.dto.AddressDTO" parameterType="java.lang.Long">
        select receiver_name, receiver_phone, province, city, district, detail_address from user_address where id = #{id}
    </select>
</mapper>
