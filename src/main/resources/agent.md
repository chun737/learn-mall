外部服务调用
1. 副作用必须幂等。任何“做了就会改变外部状态”的调用
2. 2. 失败语义必须保留。返回 Result[T, ErrorCode]，
      错误用具名常量区分。异常只用于“本不该发生”的状态。
3. 4. 不可重试的错误立即返回。4xx（429 除外）、Unauthorized、BadRequest
      不消耗重试次数。
修改内容
1.每次修改项目文件内容时都要把详细的原因和修改的逻辑说出来
2.修改时标明修改了我文件的哪行内容
3.对于数据库的增删改查只能使用sql语句